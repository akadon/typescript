name: 'build-test'
on:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - run: |
          npm install
  test: 
    runs-on: ubuntu-latest
    steps:
      - run: |
          npm install
          